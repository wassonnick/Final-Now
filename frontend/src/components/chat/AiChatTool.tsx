import { useEffect, useRef, useState } from 'react';
import { Bot, Loader2, RotateCcw, Send, ShieldCheck } from 'lucide-react';
import { Link } from 'react-router-dom';
import { Button } from '@/components/ui/button';
import { API_BASE_URL } from '@/services/backendApi';

type Entity = { type: 'society' | 'property'; id: number; name: string; url: string };
type Message = { id?: number; role: 'user' | 'assistant'; content: string; context_entities?: Entity[] | null };
const TOKEN_KEY = 'sf_ai_chat_token_v1';

export function AiChatTool() {
  const [messages, setMessages] = useState<Message[]>([{ role: 'assistant', content: 'Tell me your Gurgaon budget, BHK, preferred sector and whether you want to rent or buy. I only use published SocietyFlats inventory for specific recommendations.' }]);
  const [input, setInput] = useState(''); const [loading, setLoading] = useState(false); const [error, setError] = useState(''); const endRef = useRef<HTMLDivElement>(null);
  const token = () => window.localStorage.getItem(TOKEN_KEY) || '';
  useEffect(() => { const saved = token(); if (!saved) return; fetch(`${API_BASE_URL}/ai/chat/${saved}`).then(async (r) => r.ok ? r.json() : null).then((j) => { if (j?.data?.length) setMessages(j.data); }).catch(() => undefined); }, []);
  useEffect(() => { endRef.current?.scrollIntoView({ behavior: 'smooth' }); }, [messages, loading]);
  const send = async () => { const text = input.trim(); if (!text || loading) return; setInput(''); setError(''); setLoading(true); setMessages((items) => [...items, { role: 'user', content: text }]); try { const response = await fetch(`${API_BASE_URL}/ai/chat`, { method: 'POST', headers: { Accept: 'application/json', 'Content-Type': 'application/json' }, body: JSON.stringify({ message: text, conversation_token: token() || undefined }) }); const json = await response.json().catch(() => ({})); if (!response.ok) throw new Error(json.message || 'Chat is unavailable.'); if (json.conversation_token) window.localStorage.setItem(TOKEN_KEY, json.conversation_token); setMessages((items) => [...items, { role: 'assistant', content: json.reply, context_entities: json.entities || [] }]); } catch (e) { setError(e instanceof Error ? e.message : 'Chat is unavailable.'); } finally { setLoading(false); } };
  const reset = () => { window.localStorage.removeItem(TOKEN_KEY); setMessages([{ role: 'assistant', content: 'New chat started. What kind of Gurgaon home are you looking for?' }]); setError(''); };
  return <div className="grid gap-5 lg:grid-cols-[minmax(0,1fr)_330px]"><section className="rounded-[2rem] border border-navy-100 bg-white p-4 shadow-soft md:p-6"><div className="flex items-center justify-between gap-3"><div className="flex items-center gap-3"><span className="rounded-2xl bg-blue-50 p-3 text-blue-700"><Bot className="h-5 w-5" /></span><div><h2 className="font-black text-navy-950">SocietyFlats AI chat</h2><p className="text-xs text-navy-500">Grounded in published inventory</p></div></div><Button variant="ghost" size="sm" className="rounded-full" onClick={reset}><RotateCcw className="mr-2 h-4 w-4" />New chat</Button></div><div className="mt-5 h-[430px] space-y-4 overflow-y-auto rounded-2xl bg-ivory-100 p-4">{messages.map((message, index) => <div key={message.id || index} className={message.role === 'user' ? 'ml-auto max-w-[85%]' : 'mr-auto max-w-[90%]'}><div className={`whitespace-pre-wrap rounded-2xl px-4 py-3 text-sm leading-6 ${message.role === 'user' ? 'bg-blue-700 text-white' : 'border border-navy-100 bg-white text-navy-700'}`}>{message.content}</div>{message.context_entities?.length ? <div className="mt-2 flex flex-wrap gap-2">{message.context_entities.slice(0, 6).map((entity) => <Link key={`${entity.type}-${entity.id}`} to={entity.url} className="rounded-full border border-blue-100 bg-white px-3 py-1 text-xs font-bold text-blue-700">{entity.name}</Link>)}</div> : null}</div>)}{loading ? <div className="flex items-center gap-2 text-sm text-navy-500"><Loader2 className="h-4 w-4 animate-spin" />Checking published data…</div> : null}<div ref={endRef} /></div><div className="mt-4 flex gap-2"><textarea maxLength={1500} value={input} onChange={(e) => setInput(e.target.value)} onKeyDown={(e) => { if (e.key === 'Enter' && !e.shiftKey) { e.preventDefault(); void send(); } }} placeholder="Example: 3 BHK rental near Sector 65 under ₹1 lakh" className="min-h-12 flex-1 resize-none rounded-2xl border border-navy-100 p-3 text-sm outline-none focus:border-blue-300" /><Button disabled={loading || !input.trim()} onClick={() => void send()} className="h-12 rounded-full bg-blue-700 px-5"><Send className="h-4 w-4" /></Button></div>{error ? <p className="mt-2 text-sm font-semibold text-rose-600">{error}</p> : null}</section><aside className="rounded-[2rem] border border-emerald-100 bg-emerald-50 p-6"><ShieldCheck className="h-8 w-8 text-emerald-700" /><h2 className="mt-4 text-xl font-black text-navy-950">Evidence boundary</h2><ul className="mt-4 space-y-3 text-sm leading-6 text-navy-600"><li>Specific recommendations use published societies and live properties only.</li><li>Draft imports, leads, accounts and admin notes are never supplied to chat.</li><li>Chats expire after 30 days and are protected by a private browser token.</li><li>AI output can be wrong; verify pricing and availability on the linked profile.</li></ul><Button asChild variant="outline" className="mt-6 rounded-full bg-white"><Link to="/search">Browse verified search</Link></Button></aside></div>;
}
